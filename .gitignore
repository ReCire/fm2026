node_modules/
dist/
.vercel/
.DS_Store
*.local
.env
.env.*
!.env.example
coverage/
playwright-report/
test-results/
docs/screenshots/*.png
.svelte-kit/
app/build/
app/.vercel/

# Reference material dropped in by hand, not part of this project.
# Ignored so a broad `git add` cannot sweep 8 MB of someone else's repo in —
# which has already happened twice here with generated directories.
traumverein_app/

# Subagent worktrees. Working copies, not history.
.claude/worktrees/
